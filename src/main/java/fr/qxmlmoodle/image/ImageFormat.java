package fr.qxmlmoodle.image;

/** Enumeration ImageFormat represent the different type of XML moodle image. */
public enum ImageFormat {
    /** BASE64 image encoded in base64. */
    BASE64,
    /** image URL. */
    URL
}
